---
# cc-bestpractice 文档元数据
domain: "{领域名称}"
type: "project-standard"
project: "{项目名}"
version: "1.0.0"
created: "{日期}"
updated: "{日期}"
sources:
  web: []
  project: []
  user: []
template: "project-standard-template@1.0"
# ★v1.11 深度层配置（项目规范默认纯规范风格，深度层默认关闭）
depth_layers:
  style: "pure-standard"  # 项目规范默认纯规范风格
  D3_principle: false
  D4_evolution: false
  D5_mental_model: false
  D6_controversy: false   # 项目规范是内部约定，不讲争议
  D7_direction: false
---

<!-- project-standard-template: 项目规范模板（项目适配优先）。
     用户说"项目X规范"/"X项目编码规范"时使用此模板。信息源权重：代码 > 已有文档 > web搜索。 -->
<!-- 填充者（Generate agent）必读：
     1. 本模板的核心特征是"项目特化"——每条规范必须标注来源：【行业标准】或【项目特化】（门禁1具体性的项目版）。
        - 【行业标准】= 复用领域通用实践（如 PEP8 / 官方 style guide），照搬即可，只需确认项目无相反配置。
        - 【项目特化】= 项目自身约定（出于历史/团队/技术栈原因偏离标准），必须给"原因"。
     2. 第 5 章"与行业标准的差异"表格是本模板的强制产物——没有差异就不算项目规范（只是行业规范的复述）。
        差异数量 ≥ 3 条；每条"原因"列必须有实质理由，禁止"团队习惯"式空话。
     3. 第 4 章"工具链配置"必须给完整可复制的配置文件占位（lint/format/build/test 四类齐全），
        让读者能直接 copy-paste 到项目根目录（过可操作性门禁/门禁5）。
     4. 关键事实声明（版本号/依赖版本/配置项）必须标来源（项目文件路径 / 官方文档），零容忍（门禁3）。
        ★v1.11 数字三件套：量化数字附 值+来源URL+口径，缺一删除。
     5. 项目扫描结果应优先消费：pyproject.toml / package.json / go.mod 等锁文件的实际版本，而非"推荐版本"。
     6. ★修正清单检查：生成前过 SKILL.md「已知领域修正清单」，本领域有已知坑则逐条核对（防回归）。
     7. ★v1.11 风格说明：默认纯规范风格（权威口吻/反例列表）。
        用户说"博客/写给人看"→ 切橙皮书风格，激活六件套。
     8. ★v1.11 六件套（仅橙皮书风格强制）：纯规范走①-④，不强制⑤推荐框/⑥核心建议框。
-->

# {项目} {领域} 规范

> 版本 {version} | 更新于 {date} | 项目: {project} | 领域: {domain}
> 来源: {sources 摘要，project 路径优先}
> 置信度图例: ★★★ 官方+共识 / ★★ 主流未官方 or 单一可信源 / ★ 个人经验 or 项目经验 or 无来源 or 未量化

---

## 1. 项目上下文

<!-- 填充指令：本章是"项目适配"的锚——所有后续规范都基于本章的技术栈和现有配置。
     必须从项目实际文件扫描而来（pyproject.toml/package.json/go.mod/Makefile/已有 lint 配置），
     禁止凭"假设这个项目用 X"编造（违反来源门禁/门禁3）。 -->

### 1.1 技术栈（版本 + 依赖列表）

<!-- 填充指令：列出主语言/框架的精确版本（从锁文件读，非"推荐"）+ 核心依赖列表（直接依赖，非传递依赖）。
     每个版本号必须标来源文件路径。 -->
<!-- 占位格式：
     主语言: {语言} {version}（来源: <lockfile 路径>） [置信度:★★★]
     框架: {框架} {version}（来源: <lockfile 路径>） [置信度:★★★]
     核心依赖（直接依赖，非传递）:
       - <pkg>@<version> — <用途一句话>（来源: <lockfile>）
       - <pkg>@<version> — <用途一句话>
     Lint/Format/Test 工具版本:
       - <tool>@<version>（来源: <devDependencies 或 dev 组>）
-->

### 1.2 现有规范/配置（已有工具 + 已有规则）

<!-- 填充指令：扫描项目已有的 lint/format/test/editor 配置文件，逐一列出文件路径 + 关键规则摘要。
     这决定了"第 2 章规范"是"继承现状"还是"补充缺失"。 -->
<!-- 占位格式：
     已有配置文件:
       - <path>（工具: <tool>）— 关键规则: <rule 摘要> [置信度:★★★]
       - <path>（工具: <tool>）— 关键规则: <rule 摘要>
     编辑器配置: <.editorconfig / .vscode/settings.json 存在性 + 摘要>
     CI 配置: <.github/workflows 或 .gitlab-ci.yml 存在性 + 检查项摘要>
     缺失项（项目未配置但应配置）: <列举，作为第 4 章工具链配置的补充依据>
-->

### 1.3 本规范补充项（对比行业标准的差异化）

<!-- 填充指令：用 3-5 句概述本项目相对行业标准的偏离方向（偏严 / 偏松 / 完全自定义）。
     这是第 5 章详细差异表的"导言"。具体差异条目放第 5 章，本章只做方向性总结。 -->
<!-- 占位：
     总体偏离方向: <偏严/偏松/自定义>，主要差异领域: <命名/类型/测试覆盖/架构> [置信度:★★]
     关键补充项（项目特有，行业标准未覆盖）:
       - <补充项1，如"内部 RPC 错误码规范"> — <为什么需要>
       - <补充项2> — <为什么需要>
-->

---

## 2. 项目编码规范

<!-- 填充指令：本章是核心。每条规范必须显式标注来源标签：`【行业标准】` 或 `【项目特化】`。
     - 【行业标准】条目：照搬领域通用实践，附 PEP/官方 style guide 来源，无需理由。
     - 【项目特化】条目：必须给"项目特化理由"（历史包袱/团队约定/技术栈约束/性能要求），无理由的降级为行业标准。
     覆盖：命名/格式/类型/错误处理/注释 5 类至少各 1 条。 -->

### 2.1 命名规范

<!-- 填充指令：覆盖变量/函数/类/常量/文件/模块至少 4 类，每类标来源标签 + 正例 + 反例。
     项目特化项（如内部命名约定）必须给理由。 -->
<!-- 占位格式（每类一条）：
     函数命名 snake_case 【行业标准】 [置信度:★★★] [来源:<PEP8/官方>]
       正例：`def get_user_info():`
       反例：`def getUserInfo():`
     内部类型前缀 `T` 【项目特化】 [置信度:★★] [来源:<项目内部约定>]
       正例：`class TUserDTO:`
       反例：`class UserDTO:`
       特化理由：<历史代码库沿用此约定，全量改名成本高；新增类型必须遵循>
-->

### 2.2 格式规范

<!-- 填充指令：缩进/行宽/引号/分号等。优先复用项目已有 formatter 配置（1.2 节扫到的）。
     项目特化的格式规则（如行宽设为非默认值）必须给理由。 -->
<!-- 占位格式：
     行宽 {N} 字符 【项目特化】 [置信度:★★] [来源:<.editorconfig 或 formatter 配置>]
       正例：遵守 {N} 列
       反例：超出 {N} 列未换行
       特化理由：<团队显示器/分屏/历史代码库平均行长，与行业默认 79/120 不同>
-->

### 2.3 类型系统

<!-- 填充指令：类型注解严格度 + 项目特化的类型模式（如自定义泛型、Protocol 约定）。
     若项目禁用某些类型特性（如禁用 Any），标【项目特化】并给理由。 -->
<!-- 占位格式：
     公开 API 必须有完整类型注解 【行业标准】 [置信度:★★★] [来源:<PEP 484>]
     禁用 `Any`，必须用具体类型或 `object` 【项目特化】 [置信度:★★]
       特化理由：<项目曾因 Any 导致运行时崩溃，CI 加 mypy strict>
-->

### 2.4 错误处理

<!-- 填充指令：异常分类 + 项目特定的错误码/错误类型体系（若有）。
     必含"裸 except / 吞异常"反例（典型门禁2 检查点）。 -->
<!-- 占位格式：
     禁止裸 `except:` 【行业标准】 [置信度:★★★] [来源:<官方 tutorial>]
       正例：`except SpecificError as e:`
       反例：`except: pass`
     内部异常必须转 {项目错误类型} 再抛出 【项目特化】 [置信度:★★]
       特化理由：<统一错误边界，API 层只暴露项目错误类型>
-->

### 2.5 注释与文档

<!-- 填充指令：docstring 格式（Google/NumPy/Sphinx）+ 项目特化的注释要求（如必须标注 issue 号）。
     docstring 格式选择若是项目约定，标【项目特化】。 -->
<!-- 占位格式：
     公开函数用 Google 风格 docstring 【项目特化】 [置信度:★★] [来源:<项目内部约定>]
       特化理由：<团队工具链基于 Google 风格解析>
     TODO 必须带 issue 号 `# TODO(#123):` 【项目特化】 [置信度:★★]
       特化理由：<CI 校验未关闭 issue 的 TODO，防止僵尸 TODO>
-->

---

## 3. 项目架构规范

<!-- 填充指令：项目特定的架构约束，每条给出"约束 + 理由"。
     理由必须具体（性能/历史/团队规模/安全），禁止"为了清晰"式空话（门禁1）。
     可引用 .2pp/{run}/50-decision.md 的 ADR（若有 cc-2pp 产出）。 -->

### 3.1 目录结构约束

<!-- 填充指令：ASCII art 目录树 + 关键目录的职责约束（哪些文件必须放哪、禁止跨目录放）。 -->
<!-- 占位：
     推荐目录结构:
     ```
     {project}/
     ├── src/{pkg}/        # <职责>
     ├── tests/            # <职责>
     └── scripts/          # <职责>
     ```
     约束: <约束条目> [置信度:★★] 理由: <具体理由>
-->

### 3.2 分层与依赖方向

<!-- 填充指令：若项目分层（API/Service/Domain/Infra），明确依赖方向 + 禁止的反向依赖。
     反例必须有。 -->
<!-- 占位：
     依赖方向: API → Service → Domain → Infra（单向向内）【项目特化】 [置信度:★★]
       正例：Service 调 Domain 接口
       反例：Domain import Service → 循环依赖（lint 规则禁止）
       理由:<DDD 分层，保证 Domain 可独立测试>
-->

### 3.3 跨模块约定

<!-- 填充指令：模块间通信约定（同步调用/事件/共享类型）、共享代码归属。
     项目特定的约定（如内部 RPC schema 归属）必须列。 -->
<!-- 占位：
     跨模块通信走 {方式} 【项目特化】 [置信度:★★]
       理由: <具体约束原因>
-->

---

## 4. 工具链配置

<!-- 填充指令：本章必须给完整可复制的配置文件占位，让读者能直接 copy-paste 到项目根目录。
     四类齐全：lint / format / build / test。配置项从项目实际文件（1.2 节扫到的）提炼，
     而非编造。每段配置前注明文件路径 + 工具 + 版本。 -->

### 4.1 Lint 配置

<!-- 填充指令：完整可复制的 lint 配置文件占位。工具+版本必须标（从 1.1 节依赖列表读）。
     规则集注释每条规则的目的（关键规则才注，非每条）。 -->
<!-- 占位：
     文件: <path>（工具: <tool>@<version>） [置信度:★★★]
     ```<config 格式>
     # <tool> 配置
     <可复制的完整配置内容>
     ```
     关键规则说明:
       - <rule>: <作用一句话>
-->

### 4.2 Format 配置

<!-- 填充指令：完整可复制的 formatter 配置。强调与 lint 的协作（formatter 管格式，lint 管代码质量，不重叠）。 -->
<!-- 占位：
     文件: <path>（工具: <tool>@<version>） [置信度:★★★]
     ```<config 格式>
     <可复制的完整配置内容>
     ```
-->

### 4.3 Build 配置

<!-- 填充指令：构建工具配置（如 pyproject.toml [build-system]、Makefile build target、package.json scripts.build）。
     给可复制片段。若无独立 build（解释型语言），说明"无独立 build 步骤，部署即安装依赖"。 -->
<!-- 占位：
     文件: <path>（工具: <tool>@<version>） [置信度:★★★]
     ```<config 格式>
     <可复制的 build 配置内容>
     ```
-->

### 4.4 Test 配置

<!-- 填充指令：测试工具配置 + 覆盖率阈值（若项目特化，标【项目特化】给理由）。
     给可复制片段。 -->
<!-- 占位：
     文件: <path>（工具: <tool>@<version>） [置信度:★★★]
     ```<config 格式>
     <可复制的 test 配置内容，含覆盖率阈值>
     ```
     覆盖率阈值: {N}% 【项目特化/行业标准】 [置信度:★★]
       （若特化）理由: <为什么不是行业默认 80%>
-->

---

## 5. 与行业标准的差异

<!-- 填充指令：本章是 project-standard 模板的强制产物，没有差异就不算项目规范。
     - 差异数量 ≥ 3 条（门禁：项目特化门禁）。
     - 每条"原因"列必须有实质理由（历史/性能/团队/安全），禁止"团队习惯"式空话。
     - 表格四列齐全：条目 | 行业标准 | 本项目 | 原因。
     - 无差异的条目也要承认（"本项目与行业标准一致"），但聚焦于差异行。 -->

| 条目 | 行业标准 | 本项目 | 原因 |
|------|---------|--------|------|
| <差异条目1，如行宽> | <79/120 字符> [来源:<PEP8>] | <{N} 字符> [来源:<项目配置>] | <具体理由：显示器/历史代码/团队> [置信度:★★] |
| <差异条目2，如类型严格度> | <宽松 mypy> [来源:<官方>] | <mypy strict + 禁 Any> [来源:<pyproject.toml>] | <具体理由：曾因 Any 线上崩溃> [置信度:★★] |
| <差异条目3，如测试覆盖率> | <80%> [来源:<社区共识>] | <{N}%> [来源:<CI 配置>] | <具体理由> [置信度:★★] |
| <差异条目4> | <标准做法> [来源:<来源>] | <项目做法> [来源:<配置路径>] | <具体理由> [置信度:★] |
| <差异条目5> | <标准做法> [来源:<来源>] | <项目做法> [来源:<配置路径>] | <具体理由> [置信度:★] |
<!-- 可追加更多行，差异 ≥ 3 条即可。无差异的关键项也建议列出（标注"一致"），证明做过对比。 -->

<!-- 差异总结（1-2 句，放在表格后）：
     本项目相对行业标准的整体偏离是 <偏严/偏松/自定义>，核心驱动因素是 <性能/历史/团队规模/安全>。
     这些差异不是任意选择，每条都有上表"原因"列的具体约束驱动。
-->

---

## 附录 A: 快速接入检查清单

<!-- 填充指令：新成员加入项目或新代码合入前的快速自检清单。
     每项可 grep / 可看配置，禁空话。覆盖编码 + 架构 + 工具链三类，10-15 项。 -->

### A.1 编码与架构检查项

- [ ] 所有公开函数/方法有类型注解（grep 验证） [置信度:★★★]
- [ ] 无裸 `except`（grep 验证）
- [ ] 命名符合 2.1 节约定（含项目特化前缀，若有）
- [ ] 依赖方向单向（3.2 节，lint 规则校验）
- [ ] TODO 带 issue 号（2.5 节，grep `# TODO(#`）

### A.2 工具链检查项

- [ ] lint 通过：`<lint cmd>` 退出码 0
- [ ] format 通过：`<format cmd --check>` 退出码 0
- [ ] test 通过：`<test cmd>` 退出码 0
- [ ] 覆盖率达标：≥ {N}%（4.4 节阈值）
- [ ] 新依赖已加入锁文件（1.1 节路径）

---

## 附录 B: 更新日志

<!-- 填充指令：首次生成写第一行；增量更新（mode=update）按版本语义表追加。
     版本语义：patch=新增补充 / minor=过时重写 / major=模板升级重构。 -->

| 日期 | 版本 | 变更 | 来源 |
|------|------|------|------|
| {YYYY-MM-DD} | 1.0.0 | 首次生成（project-standard-template@1.0） | {sources 摘要，project 路径优先} |
<!-- 增量更新追加行示例：
| 2026-09-20 | 1.0.1 | 新增内部错误码规范（patch） | 项目内部约定 |
| 2026-12-01 | 1.1.0 | 重写架构分层（minor，引入 DDD 分层） | .2pp/.../50-decision.md |
-->
