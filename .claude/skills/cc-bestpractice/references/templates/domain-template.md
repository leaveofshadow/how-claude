---
# cc-bestpractice 文档元数据
domain: "{领域名称}"
type: "domain-guide"
version: "1.0.0"
created: "{日期}"
updated: "{日期}"
sources:
  web: []
  project: []
  user: []
template: "domain-template@1.0"
# ★v1.11 深度层配置（Parse阶段按矩阵填写，Generate时遵守）
depth_layers:
  style: "orange-book"  # 领域指南默认橙皮书风格
  D3_principle: true     # 原理层（必含）
  D4_evolution: true     # 演变层（必含）
  D5_mental_model: true  # 心智模型（必含，过反直觉测试）
  D6_controversy: "optional"  # 争议层（选含：用户说选型/对比 或 Research发现社区分裂时开）
  D7_direction: true     # 方向层（必含）
---

<!-- domain-template: 领域指南模板（深度优先）。用户说"X最佳实践"时使用此模板。 -->
<!-- 填充者（Generate agent）必读：
     1. 每条推荐必须满足 γ三问：具体吗？可操作吗？有证据吗？三问全过标 ★★★，否则降级。
     2. 每个规范条目必须含正例 + 反例占位，违反反例门禁（门禁2）即不合格。
     3. 关键事实声明（版本号/特性/性能数字）必须标来源（官方/社区/版本），零容忍（门禁3）。
        ★v1.11 数字三件套：所有量化数字附 值+来源URL+统计口径/年份，缺一删除（防数字幻觉）。
     4. 反模式清单 ≥ 8 条；橙皮书风格时用踩坑故事格式（坑→现象→根因→推荐做法），每条配推荐框/不推荐框。
        检查清单 checkbox ≥ 25 项（附录 A 分 3 类，每类 10-15 项）。
     5. 每个章节末尾留"可操作步骤"三件套（装什么/写什么/改什么），过可操作性门禁（门禁5）。
     6. ★v1.10.2 修正清单检查（防历史坑回归）：生成前先过 SKILL.md「已知领域修正清单」，
        若本领域有已知坑（如 FastAPI 的 passlib/jose），逐条核对，不重蹈。
        通用修正（所有领域必查）：版本基线已 web 核实 / 引用库当前状态已核实（废弃?CVE?移除?）/ 代码 import 自洽。
     7. ★v1.10.3 教程三件套（能抄能用）：①任务一加 3-5 行最小可跑入口示例+启动命令+验证；
        ②每个新概念第一次出现先一句话解释（优先用类比，禁纯术语）；③附录加一个端到端组装示例（完整业务闭环，不只单一操作）。
     8. ★v1.11 六件套（橙皮书风格强制）：
        ①入口示例（任务一）②概念解释层（每个新概念）③组装示例（末尾，完整闭环）
        ④过渡叙事（关键跳跃处）⑤推荐框/不推荐框（每条核心规范）⑥核心建议框（每大节末尾 `> ` 引用块）。
     9. ★v1.11 深度层（按 depth_layers 配置）：
        D3 原理层：核心技术解释底层机制（不是定义复述），2-3句+架构差异+知道后你会做的不同决策。
        D4 演变层：3-5句叙事（从哪来→现在哪阶段→近期变化带版本），不是时间线列表。
        D5 心智模型：3-5层导航框架，过反直觉测试（换成目录读者预期会变？不变=删）。放概述后正文前。
        D6 争议（若开）：嵌入对应推荐，只在真有对立观点时写，不硬编"但是"。
        D7 方向层：末尾3-5句，2-3趋势+哪些做法会被淘汰+关注什么信号。
     10. ★v1.11 橙皮书5步写作流：每节按 抓痛点→讲道理→给操作→留核心→防废话 写。
         痛点先行（不是定义先行）；讲道理用类比；给操作配推荐/不推荐框；留核心一句话建议；防废话删色彩词。
-->

# {领域} 最佳实践指南

> 版本 {version} | 更新于 {date} | 来源: {sources摘要}
> 置信度图例: ★★★ 官方+共识+版本佐证 / ★★ 主流但未官方 or 单一可信源 / ★ 个人经验 or 小众 or 无来源 or 未量化

---

## 0. 金句与心智模型（★v1.11 — 开篇抓住读者，橙皮书风格必含）

### 0.0 一句话核心（金句）
{一句话点出本领域的核心洞察，不是描述。如"FastAPI不是更快的Flask，是会自动写文档的Python类型系统"。}

### 0.1 演变（D4 演变层）
{3-5句叙事：这个领域从哪来→现在什么阶段→近期最值得关注的变化。带版本/时间。}

### 0.2 心智模型（D5 心智模型）
{3-5层导航框架，每层关键词+一句话。过反直觉测试：换成目录读者预期会变吗？}
```
{层1} → {一句话}
  ↓ {因果关系}
{层2} → {一句话}
  ↓
{层3} → {一句话}
```

---

## 1. 概述

### 1.1 本文档覆盖范围

<!-- 填充指令：用 2-3 句说明本文档涵盖 {领域} 的哪些子领域（对应 scope 字段）、
     明确不覆盖什么（划边界）。必须含至少 1 个版本号锚定（如"覆盖 Python 3.12+"）。 -->
<!-- 示例占位：本文档覆盖 {领域} {version}+ 的 [后端 API / 类型系统 / 异步 / 测试 / 部署]，
     不覆盖 [特定框架的内部实现 / 历史版本迁移细节]。 [置信度:★★★] -->

### 1.2 目标读者

<!-- 填充指令：列出 2-3 类读者画像 + 各自能用文档做什么。避免"所有开发者"式空话。 -->
<!-- 示例占位：
     - 初级：照着第 2 章装环境 + 第 3 章写第一段符合规范的代码
     - 中级：照着第 9 章反模式清单做 code review
     - 高级：照着第 4 章做架构选型 + 附录 B 做迁移 [置信度:★★] -->

### 1.3 核心原则（3-5 条，每条一句话 + 一个具体做法）

<!-- 填充指令：每条原则 = 一句话原则 + 一个可执行做法（命令/配置/代码片段）。
     禁止"应该注意代码质量"式空话（违反门禁1具体性）。每条标置信度。 -->
<!-- 占位格式：
     1. 原则文本 [置信度:★★★]
        做法：`<具体命令或代码片段>` [来源: <官方/社区>]
     2. ...

### 章节可操作步骤（三件套）
- 装什么：<工具名+版本+安装命令>
- 写什么：<第一个符合规范的最小代码/配置>
- 改什么：<把现有 X 改为 Y 的检查命令>
-->

---

## 2. 环境与工具链

### 2.1 版本选择（推荐版本 + 理由 + 迁移注意）

<!-- 填充指令：列出推荐版本（精确到 minor）+ 选它的 1 条具体理由（必须带来源）+ 迁移注意 1 条。
     版本声明零容忍无来源（门禁3）。 -->
<!-- 占位格式：
     推荐：{领域} {version}（来源：<官方 release notes URL>） [置信度:★★★]
     理由：<具体特性/安全修复/性能数字>
     迁移注意：<废弃 API / breaking change 一条>
-->

### 2.2 包管理（推荐工具 + 配置示例 + 锁文件策略）

<!-- 填充指令：推荐 1 个主工具 + 1 个备选，附最小可复制配置片段 + 锁文件提交策略（提交 or 忽略）。 -->
<!-- 正例占位：`<工具> install <pkg> && <工具> lock` → 提交 lock 文件 [置信度:★★★]
     反例占位：`<工具> install <pkg>` 不锁版本 → 依赖漂移（来源：<社区 issue>） -->

### 2.3 开发环境（编辑器配置 + linter + formatter + 具体配置命令）

<!-- 填充指令：linter/formatter 必须给工具名+版本+初始化命令（过门禁5可操作性）。
     配置文件给可复制的片段占位。 -->
<!-- 正例占位：`pip install ruff==<ver> && ruff init` [置信度:★★★]
     反例占位：手动检查风格 → 不可重复（无来源） -->

### 2.4 CI/CD 集成（推荐流水线 + 配置片段）

<!-- 填充指令：给出至少 3 个阶段（lint/test/build）的 pipeline 配置占位（YAML 片段）。 -->
<!-- 占位：lint → test → build → deploy，配置片段见 <CI 平台> 文档 [置信度:★★] -->

---

## 3. 编码规范

### 3.1 命名约定（每种元素的命名规则 + 正例/反例）

<!-- 填充指令：覆盖变量/函数/类/常量/模块/文件至少 4 类元素。每类给正例+反例。 -->
<!-- 占位格式（每类一条）：
     类名 PascalCase [置信度:★★★] [来源:<PEP8/官方>]
       正例：`UserService`
       反例：`userService` / `user_service`
-->

### 3.2 代码组织（文件结构 + 模块划分 + 导入顺序 + 示例）

<!-- 填充指令：给出推荐目录树（ASCII art）+ 导入顺序规则（标准库/第三方/本地三段式）。
     反例：循环导入或星号导入必须列一条。 -->
<!-- 正例占位：标准库 → 第三方 → 本地，每组间空行 [置信度:★★★]
     反例占位：`from x import *` → 污染命名空间（来源：<lint 规则 R0xxx>） -->

### 3.3 类型系统（类型注解规范 + 常见类型模式 + 正例/反例）

<!-- 填充指令：给至少 3 个常见类型模式（可选类型/泛型/联合类型）+ 校验工具命令。
     类型相关断言必须标来源（如 PEP 484）。 -->
<!-- 正例占位：`def f(x: int | None) -> str:` （{version}+） [置信度:★★★] [来源:<PEP 604>]
     反例占位：`def f(x):` → 无注解，mypy 报错 <code> -->

### 3.4 错误处理（错误分类 + 处理策略 + 正例/反例）

<!-- 填充指令：分类为 可恢复/不可恢复/编程错误 三类，各给处理策略。
     必含"裸 except / 吞异常"反例（典型门禁2 检查点）。 -->
<!-- 正例占位：`except ValueError as e: log(e); raise` [置信度:★★★]
     反例占位：`except: pass` → 吞掉 KeyboardInterrupt/SystemExit（来源：<官方 tutorial>） -->

### 3.5 异步模式（何时异步 + 模式选择 + 正例/反例）

<!-- 填充指令：给"何时该用异步"判断标准（I/O 密集 vs CPU 密集）+ 1 个正确模式 + 1 个反模式。
     若领域无异步概念，改填"并发模型"（线程/进程/协程）。 -->
<!-- 正例占位：I/O 密集 → async；CPU 密集 → 多进程 [置信度:★★]
     反例占位：CPU 密集任务用 async → 阻塞事件循环（来源：<官方 asyncio 文档>） -->

---

## 4. 架构模式

### 4.1 推荐架构（架构图 + 适用场景 + 优劣）

<!-- 填充指令：ASCII art 架构图 + 1 个适用场景 + 1 条优势 + 1 条劣势。禁只讲优势。 -->
<!-- 占位：分层/六边形/事件驱动 等，附 <规模/团队> 适用判断 [置信度:★★] -->

### 4.2 分层策略（层次定义 + 职责边界 + 依赖方向）

<!-- 填充指令：每层一句话职责 + 依赖方向（外层依赖内层，禁止反向）。
     反例：领域层直接依赖框架必须列。 -->
<!-- 正例占位：UI → Application → Domain → Infra，依赖向内 [置信度:★★★]
     反例占位：Domain import Django → 领域耦合框架（来源：<DDD 文献>） -->

### 4.3 状态管理（状态分类 + 推荐方案 + 正例/反例）

<!-- 填充指令：状态分 本地/共享/持久 三类，各给方案。反例：全局可变状态必须列。 -->
<!-- 正例占位：共享状态用 <方案> + 显式生命周期 [置信度:★★]
     反例占位：模块级可变 dict 当全局缓存 → 并发不安全（来源：<社区 issue>） -->

### 4.4 API 设计（风格选择 + 版本策略 + 正例/反例）

<!-- 填充指令：选 REST/gRPC/GraphQL 之一 + 版本策略（URL/Header/MediaType）。
     每个推荐附契约示例。 -->
<!-- 正例占位：版本放 URL `/v1/` + 向后兼容窗口 [置信度:★★★]
     反例占位：breaking change 不升版本号 → 客户端炸（来源：<REST 成熟度模型>） -->

---

## 5. 测试策略

### 5.1 测试金字塔（各层比例 + 工具选择 + 配置）

<!-- 填充指令：给金字塔比例（单测:集成:E2E ≈ 70:20:10）+ 每层工具+版本+配置命令。 -->
<!-- 占位：pytest <ver> + coverage <ver>，配置 `pytest --cov=<pkg> --cov-fail-under=80` [置信度:★★★] -->

### 5.2 关键测试模式（3-5 个必测场景 + 测试代码示例）

<!-- 填充指令：列 3-5 个必测场景（边界值/异常路径/幂等/并发），每个给可复制的测试代码块。 -->
<!-- 占位（每场景一个代码块）：
```<lang>
def test_<scenario>():
    # given / when / then
```
[置信度:★★★] -->

### 5.3 Mock 策略（何时 mock + 如何 mock + 反模式）

<!-- 填充指令：mock 边界（只 mock 外部依赖：DB/HTTP/时间）+ 反模式（mock 被测对象本身）。 -->
<!-- 正例占位：只 mock 跨进程边界（HTTP/DB） [置信度:★★★]
     反例占位：mock 被测类内部方法 → 测试无意义（来源：<社区测试反模式>） -->

---

## 6. 性能优化

### 6.1 性能基准（关键指标 + 测量工具 + 命令）

<!-- 填充指令：列 2-3 个关键指标（延迟 P99/吞吐/RSS）+ 测量工具+命令。性能数字必须标来源。 -->
<!-- 占位：P99 <Xms>（基准：<benchmark URL>），测量 `<tool> <cmd>` [置信度:★★] -->

### 6.2 常见瓶颈 + 优化方案（场景 → 瓶颈 → 方案 → 量化效果）

<!-- 填充指令：至少 3 行表格，每行四列。量化效果必须有数字（提升 X%/降低 Yms）+ 来源。 -->
<!-- 表格占位：
| 场景 | 瓶颈 | 方案 | 量化效果 |
|------|------|------|---------|
| <场景> | <瓶颈> | `<命令/改动>` | <数字>（来源:<基准>） |
-->

### 6.3 性能反模式（3-5 个 + 每个的反例 + 修正方案）

<!-- 填充指令：3-5 个性能反模式，每个给反例代码 + 修正代码 + 量化对比。 -->
<!-- 占位（每条）：
     反模式：<名称> [置信度:★★]
       反例：`<bad code>`
       修正：`<good code>`（来源：<profile 结果>）
-->

---

## 7. 安全实践

### 7.1 认证与授权（推荐方案 + 配置示例）

<!-- 填充指令：推荐 1 个 auth 方案（OAuth2/JWT/Session）+ 关键配置（过期/轮换/算法）。
     密码学相关断言必须标来源（OWASP/RFC）。 -->
<!-- 占位：JWT 用 HS256/RS256，禁 `none` 算法（来源:<RFC 7519>） [置信度:★★★] -->

### 7.2 输入验证（验证策略 + 正例/反例）

<!-- 填充指令：在信任边界做验证 + 白名单优于黑名单。反例：拼 SQL 必须列。 -->
<!-- 正例占位：参数化查询 `cursor.execute(sql, (param,))` [置信度:★★★] [来源:<OWASP>]
     反例占位：f-string 拼 SQL → SQL 注入（来源:<CWE-89>） -->

### 7.3 敏感数据（存储/传输/日志中的处理规范）

<!-- 填充指令：密码哈希算法+盐/传输 TLS/日志脱敏三点，各给具体做法。 -->
<!-- 占位：密码用 `<algo>`（如 argon2/bcrypt）+ per-user salt（来源:<OWASP cheatsheet>） [置信度:★★★] -->

### 7.4 安全反模式（3-5 个 + 每个的危害 + 修正方案）

<!-- 填充指令：3-5 个安全反模式，每个给危害（CWE 编号）+ 修正代码。 -->
<!-- 占位（每条）：
     反模式：<名称> [置信度:★★★] [来源:<CWE-xxx>]
       危害：<具体攻击向量>
       修正：`<good code>`
-->

---

## 8. 部署与运维

### 8.1 容器化（Dockerfile 最佳实践 + 示例）

<!-- 填充指令：多阶段构建 + 非 root 用户 + 最小基础镜像，附可复制 Dockerfile 片段。 -->
<!-- 占位：`FROM <base> AS build ... FROM <runtime>` + `USER nonroot`（来源:<distroless 文档>） [置信度:★★★] -->

### 8.2 配置管理（12-factor + 具体做法）

<!-- 填充指令：配置走环境变量（不进代码/不进镜像），附读取代码占位。 -->
<!-- 正例占位：`os.getenv("DB_URL")` + 启动期校验 [置信度:★★★] [来源:<12factor.net>]
     反例占位：配置硬编码 `DB_URL = "..."` → 泄密风险 -->

### 8.3 监控与可观测性（三支柱 + 工具推荐 + 配置）

<!-- 填充指令：三支柱（metrics/logs/traces）各给工具+配置片段。 -->
<!-- 占位：metrics <prometheus> / logs <structured json> / traces <otel>（来源:<CNCF 调研>） [置信度:★★] -->

### 8.4 故障排查（常见问题 → 排查命令 → 修复方案）

<!-- 填充指令：3-5 行表格：症状 → 诊断命令 → 修复。命令必须可执行。 -->
<!-- 表格占位：
| 症状 | 诊断命令 | 修复 |
|------|---------|------|
| <症状> | `<cmd>` | <方案> |
-->

---

## 9. 反模式清单

<!-- 填充指令：至少 8 条，每条四列齐全。禁止"不要写烂代码"式废话（门禁4）。
     严重度取值：高/中/低。每条尽量带来源或 CWE/PEP 编号。 -->

| # | 反模式 | 为什么错 | 正确做法 | 严重度 |
|---|--------|---------|---------|--------|
| 1 | <反模式1，如裸 except> | <吞掉所有异常含 KeyboardInterrupt> | `<except SpecificError as e:>` [来源:<官方>] | 高 |
| 2 | <反模式2，如 f-string 拼 SQL> | <SQL 注入> | `<参数化查询>` [来源:<CWE-89>] | 高 |
| 3 | <反模式3> | <原因> | <正确做法> | 高 |
| 4 | <反模式4> | <原因> | <正确做法> | 中 |
| 5 | <反模式5> | <原因> | <正确做法> | 中 |
| 6 | <反模式6> | <原因> | <正确做法> | 中 |
| 7 | <反模式7> | <原因> | <正确做法> | 低 |
| 8 | <反模式8> | <原因> | <正确做法> | 低 |
<!-- 可追加更多行，建议 ≥ 8 条。每条标注来源占位 [来源:<官方/社区>] -->

---

## 10. 工具与资源

### 10.1 推荐工具清单（分类列表 + 安装命令 + 一句话推荐理由）

<!-- 填充指令：分类（lint/format/test/security/build）每类 1-2 个工具 + 版本 + 安装命令 + 一句话理由。 -->
<!-- 占位（每条）：
     - **<工具>** `<ver>` — `<install cmd>` — <一句话理由> [置信度:★★★]
-->

### 10.2 推荐阅读（官方文档/经典书籍/社区资源 + 链接）

<!-- 填充指令：官方文档优先，每条给 URL + 一句话说明价值。链接必须真实可访问。 -->
<!-- 占位：
     - 官方文档：<title> — <URL>（<为什么必读>） [置信度:★★★]
-->

### 10.3 社区与生态（核心社区/会议/人物）

<!-- 填充指令：2-3 个核心社区/会议/代表人物，帮助读者跟进领域动态。 -->
<!-- 占位：<社区/会议/人物> — <价值说明> [置信度:★★] -->

---

## 附录 A: 检查清单（Checklist）

<!-- 填充指令：三类共 ≥ 25 项可勾选条目。每项具体可检（能 grep / 能看配置），禁空话。
     编码类 10-15 项，架构类 10-15 项，安全类 10-15 项（实际填到每类 10-12 项即可满足 ≥25）。 -->

### A.1 编码规范检查项

- [ ] 所有公开函数/方法有类型注解 [置信度:★★★]
- [ ] 无裸 `except`（grep 验证）
- [ ] 无 `from x import *`（grep 验证）
- [ ] 命名符合约定（函数 snake_case / 类 PascalCase / 常量 UPPER）
- [ ] 导入顺序：标准库 → 第三方 → 本地，每组空行
- [ ] 无硬编码配置（grep 验证无内联 URL/密钥）
- [ ] 异常被具体捕获并记录或重抛
- [ ] 公共 API 有 docstring
- [ ] 行宽符合 formatter 规则（CI 校验）
- [ ] 无循环导入（启动期检测）
- [ ] 复杂度（cyclomatic）≤ 阈值（lint 规则）
- [ ] 无 dead code（lint 规则验证）

### A.2 架构检查项

- [ ] 依赖方向单向（外层 → 内层，无反向）
- [ ] 领域层不依赖框架（grep 验证无框架 import）
- [ ] 状态管理边界清晰（无全局可变状态）
- [ ] API 版本策略明确（URL/Header/MediaType 选一）
- [ ] 模块职责单一（每个模块 < N 行或单一关注点）
- [ ] 跨层通信走定义好的接口（非直接跨层调用）
- [ ] 错误在边界转换（内部异常 → 边界错误码）
- [ ] 配置走环境变量（12-factor）
- [ ] 关键路径有架构决策记录（ADR）
- [ ] 无 God class / God module（lint 复杂度）

### A.3 安全检查项

- [ ] 密码用 argon2/bcrypt 哈希 + per-user salt
- [ ] SQL 全部参数化（grep 验证无字符串拼接）
- [ ] 输入在信任边界做验证（白名单优先）
- [ ] 依赖无已知高危漏洞（`<audit cmd>` 验证）
- [ ] 敏感数据不出现在日志（grep 验证）
- [ ] 传输强制 TLS
- [ ] JWT 算法非 `none`（配置验证）
- [ ] 错误信息不泄漏堆栈/内部结构（生产环境）
- [ ] 密钥/证书不进代码库（pre-commit 扫描）
- [ ] 最小权限：服务以非 root 运行
- [ ] 速率限制/认证覆盖所有公开端点

---

## 附录 B: 迁移指南（从旧版本/旧范式迁移）

### B.1 迁移步骤

<!-- 填充指令：分 4-6 步的有序迁移流程，每步可执行（有命令/有检查）。 -->
<!-- 占位（每步）：
     1. <步骤> — 命令 `<cmd>` — 验证 `<check cmd>` [置信度:★★]
-->

### B.2 常见迁移陷阱

<!-- 填充指令：3-5 个迁移中常踩的坑 + 规避方法。 -->
<!-- 占位：
     - 陷阱：<名称> → 规避：<做法>（来源:<迁移指南>） [置信度:★★]
-->

---

## 附录 D: 完整骨架（★v1.1必产，可运行性门禁6的依据）

<!-- ★v1.1新增：源于 FastAPI 对抗验证——"5项门禁全过却跑不起来"。
     本附录是门禁6（可运行性）的硬性产物。没有它，正文所有 import 链都是断的。 -->

<!-- 填充者（Generate agent）必读：
     1. 附录D 必须让读者「git clone 风格地」拿到一个能从零跑通的最小项目骨架。
     2. 正文引用的所有胶水函数（get_db/get_settings/User model/conftest fixture/DI工厂）
        必须在此处给完整实现，不得只留函数名。
     3. 所有文件互相 import 自洽——读附录D 的 agent 要能模拟一遍 import 链不断链。
     4. 骨架只含「跑通 hello world + 1 个真实 endpoint」所需，不堆业务逻辑。
     5. 最后给「启动验证三行」：启动命令 + 验证命令 + 期望输出。
     6. 版本号统一：附录D 与 §2.1 推荐版本、frontmatter 必须一致（过版本时效门禁7）。
-->

### D.1 项目依赖清单（完整 pyproject.toml / package.json / Cargo.toml）

<!-- 填充指令：给一份完整可用的依赖清单，含 [project] name/version/requires-python/dependencies
     + [project.optional-dependencies] dev（lint/test 全家桶）+ [tool.*] 配置段。
     这是把全文所有「装什么」命令统一到一个文件。版本号与 §2.1 对齐。 -->
<!-- 占位：
     ```toml
     # pyproject.toml（完整版，uv/poetry 通用）
     [project]
     name = "{slug}"
     version = "0.1.0"
     requires-python = ">={version}"
     dependencies = [
         "{framework}>={ver}",        # 与 §2.1 基线一致
         "{core-deps}",
     ]
     [project.optional-dependencies]
     dev = ["ruff", "mypy", "pytest", ...]
     [tool.ruff]
     ...（与 §2.3 配置一致）
     ```
     [置信度:★★★]
-->

### D.2 入口文件（main.py / index.ts / main.rs 完整实现）

<!-- 填充指令：给一个能 `uvicorn app.main:app` / `npm start` / `cargo run` 直接启动的完整入口。
     含：实例化 + 1 个 health/hello endpoint + lifespan（若有重资源）+ include_router。 -->
<!-- 占位：
     ```python
     # src/{slug}/main.py
     from {framework} import {App}, {router}
     ...
     app = {App}(...)
     @app.get("/health")
     ...
     if __name__ == "__main__":
         ...
     ```
     [置信度:★★★]
-->

### D.3 配置加载（.env.example + config 实现完整）

<!-- 填充指令：给 .env.example（所有必填变量+示例值）+ config.py 完整实现（BaseSettings/get_settings）。
     确保用户 cp .env.example .env 后能直接启动，不因缺 SECRET_KEY/DATABASE_URL 崩。 -->
<!-- 占位：
     ```bash
     # .env.example
     DATABASE_URL=...
     SECRET_KEY=...
     ```
     ```python
     # core/config.py 完整实现（含 get_settings + lru_cache）
     ```
     [置信度:★★★]
-->

### D.4 胶水函数完整实现（正文引用的所有 Depends/factory/helper）

<!-- 填充指令：列出正文所有「只引用未实现」的函数，逐一给完整实现。
     典型清单（按领域调整）：get_db/get_async_db、get_current_user、User model、Base、
     UserCreate/UserRead schema、xxx_service、conftest 的 fixture。
     自洽性自检：逐个 grep 正文 import，确保此处都有对应实现。 -->
<!-- 占位（每个函数一段）：
     ```python
     # core/db.py
     def get_async_db(): ...   # 正义完整的 yield + finally close
     ```
     [置信度:★★★]
-->

### D.5 测试骨架（conftest.py + 1 个可跑通的测试）

<!-- 填充指令：给 conftest.py（DB fixture + dependency_overrides 的 autouse 清理，避免跨文件污染）
     + 1 个针对 D.2 health endpoint 的测试，保证 `pytest` 一把过。
     ★必须含 app.dependency_overrides.clear() 的 teardown（对抗验证挖出的假绿坑）。 -->
<!-- 占位：
     ```python
     # tests/conftest.py
     @pytest.fixture(autouse=True)
     def _reset_overrides():
         yield
         app.dependency_overrides.clear()   # 防跨文件测试污染
     ...
     ```
     [置信度:★★★]
-->

### D.6 启动验证（三行：启动 + 验证 + 期望输出）

<!-- 填充指令：给最小可验证闭环。三行必须真实可执行（不是占位）。
     启动命令 + curl/请求命令 + 期望输出（含状态码+body 关键字段）。 -->
<!-- 占位：
     ```bash
     # 1. 启动
     uvicorn src.{slug}.main:app --reload      # 或 npm start / cargo run
     # 2. 验证（另开终端）
     curl http://localhost:8000/health
     # 3. 期望输出
     # {"status":"ok"}   ← HTTP 200
     ```
     [置信度:★★★]
-->

### D.7 骨架自洽性检查清单（生成后必过）

<!-- 填充指令：把以下检查项作为门禁6的自检脚本（可 grep 验证）。 -->
- [ ] D.1 依赖清单版本号 == §2.1 基线 == frontmatter（无三处不一致）
- [ ] D.2 入口文件能被 §2.1 推荐版本的解释器/运行时启动
- [ ] D.3 .env.example 覆盖所有 config 必填字段（cp 后启动不崩）
- [ ] D.4 正文每个 import 的函数都能在 D.4 找到完整实现（grep 逐个核对）
- [ ] D.5 测试含 dependency_overrides.clear()（防假绿）
- [ ] D.6 三行命令真实可执行（非占位）

---

## 附录 C: 更新日志

<!-- 填充指令：首次生成写第一行；增量更新（mode=update）按版本语义表追加。
     版本语义：patch=新增补充 / minor=过时重写 / major=模板升级重构。 -->

| 日期 | 版本 | 变更 | 来源 |
|------|------|------|------|
| {YYYY-MM-DD} | 1.0.0 | 首次生成（domain-template@1.0） | {sources 摘要} |
<!-- 增量更新追加行示例：
| 2026-09-20 | 1.0.1 | 新增 match-case 章节（patch） | python.org/docs/3.12 |
| 2026-12-01 | 1.1.0 | 重写异步章节（minor，asyncio 新 API） | PEP-0660 |
-->
